name: Release

on:
  push:
    tags:
      - "*-v*"

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always

jobs:
  build-and-release:
    name: Build release binary (${{ matrix.artifact_suffix }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            artifact_suffix: linux-x86_64
          - os: ubuntu-24.04-arm
            artifact_suffix: linux-arm64
          - os: macos-latest
            artifact_suffix: macos-arm64
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@1.89.0

      - name: Cache cargo registry and target dir
        uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.artifact_suffix }}

      - name: Extract crate name from tag
        id: crate
        run: |
          tag="${GITHUB_REF_NAME}"
          name="${tag%-v*}"
          echo "name=${name}" >> "$GITHUB_OUTPUT"

      - name: Build release binary
        run: cargo build --release -p "${{ steps.crate.outputs.name }}"

      - name: Package binary
        run: |
          set -euo pipefail
          mkdir -p dist
          src="target/release/${{ steps.crate.outputs.name }}"
          if [ ! -f "$src" ]; then
            echo "Expected binary not found: $src" >&2
            exit 1
          fi
          dest="dist/${{ steps.crate.outputs.name }}-${{ matrix.artifact_suffix }}"
          cp "$src" "$dest"
          chmod +x "$dest"

      - name: Attach binary to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*
